
# 1. GraphQL API Vulnerabilities

GraphQL vulnerabilities stem from implementation and design flaws, such as leaving introspection features active, allowing attackers to query API schemas for reconnaissance.

**Attack Types:**
- **Malicious requests** - Obtain unauthorized data or perform restricted actions
- **Query manipulation** - Gain admin privileges through crafted queries
- **CSRF exploits** - Execute cross-site request forgery attacks
- **Information disclosure** - Extract sensitive data through vulnerable endpoints

**Impact:**
These attacks can have severe consequences, particularly when attackers escalate privileges or access sensitive information through poorly secured GraphQL implementations.

## 1.1 - What is GraphQL

GraphQL is an API query language designed for efficient client-server communication. It allows users to specify exactly what data they want in responses, avoiding large response objects and multiple calls common with REST APIs.

**Key Features:**
- **Contract-based communication** - Defines how clients communicate with servers
- **Location abstraction** - Clients don't need to know where data resides
- **Single query endpoint** - GraphQL server fetches data from relevant sources
- **Platform-agnostic** - Supports multiple programming languages and data stores

GraphQL streamlines API interactions by enabling precise data requests through a unified interface.

## 1.2 - How GraphQL works

GraphQL schemas define data structure with types, fields, and relationships. Three operation types manipulate this data:
* **Queries** fetch data
* **Mutations** add, change, or remove data  
* **Subscriptions** maintain permanent connections for real-time server updates

Unlike REST APIs with multiple endpoints and HTTP methods, GraphQL uses a single endpoint (typically POST). Operations are defined by type and name rather than endpoint or HTTP method. Responses return JSON objects matching the requested structure.

## 1.3 - What is a GraphQL schema?

A GraphQL schema is a contract between frontend and backend that defines available data using a human-readable schema definition language.

Schemas primarily consist of object types that specify available objects, fields, and arguments. Field types can be objects, scalars, enums, unions, interfaces, or custom types. The `!` operator indicates non-nullable (mandatory) fields.

Example:
```
type Product {
  id: ID!
  name: String!
  description: String!
  price: Int
}
```

Schemas must include at least one query and typically contain mutation definitions.

## 1.4 - What are GraphQL queries?

GraphQL queries retrieve data from the data store, equivalent to REST API GET requests.

Key components:
* **Operation type** (`query`) - optional but recommended
* **Query name** - optional but helpful for debugging  
* **Data structure** - specifies what data to return
* **Arguments** - optional, used to target specific objects

Example:
```
query myGetProductQuery {
  getProduct(id: 123) {
    name
    description
  }
}
```

GraphQL's flexibility allows requesting only needed fields, even when the schema contains more available data.

## 1.5 - What are GraphQL mutations?

Mutations change data by adding, deleting, or editing it, equivalent to REST API's POST, PUT, and DELETE methods.

Like queries, mutations have an operation type, name, and return structure, but always require input (typically provided as variables).

Example:
```
# Request
mutation {
  createProduct(name: "Flamin' Cocktail Glasses", listed: "yes") {
    id
    name
    listed
  }
}

# Response
{
  "data": {
    "createProduct": {
      "id": 123,
      "name": "Flamin' Cocktail Glasses",
      "listed": "yes"
    }
  }
}
```

## 1.6 - Components of queries and mutations

The GraphQL syntax includes several common components for queries and mutations. (`Fields`, `Arguments`, `Variables`, `Aliases`, `Fragments`)

### Fields

GraphQL types contain queryable data items called fields. Queries and mutations specify which fields to return, and responses mirror the requested structure.

The example below shows a query to get ID and name details for all employees, and its associated response. In this case, `id`, `name.firstname`, and `name.lastname` are the fields requested.

Example:
```
# Request
query myGetEmployeeQuery {
  getEmployees {
    id
    name {
      firstname
      lastname
    }
  }
}
```

```
# Response
{
  "data": {
    "getEmployees": [
      {
        "id": 1,
        "name": {
          "firstname": "Carlos",
          "lastname": "Montoya"
        }
      },
      {
        "id": 2,
        "name": {
          "firstname": "Peter",
          "lastname": "Wiener"
        }
      }
    ]
  }
}
```

### Arguments

Arguments are values provided for specific fields, defined in the schema. They allow GraphQL servers to return specific objects rather than all objects based on the provided criteria.

Example: A `getEmployee` request with an employee ID argument returns only that employee's details instead of all employees.

```
    #Example query with arguments

    query myGetEmployeeQuery {
        getEmployees(id:1) {
            name {
                firstname
                lastname
            }
        }
    }
```

```
#Response to query

    {
        "data": {
            "getEmployees": [
            {
                "name" {
                    "firstname": Carlos,
                    "lastname": Montoya
                    }
                }
            ]
        }
    }
```

**Note:** If user-supplied arguments are used to access objects directly then a GraphQL API can be vulnerable to access control vulnerabilities such as insecure direct object references (IDOR).

### Variables

Variables pass dynamic arguments from a separate JSON dictionary instead of inline values, enabling query reuse with different values.

To use variables:
* Declare the variable and type
* Add the variable name in the query
* Pass the variable key and value from the dictionary

The example below shows the same query as in the previous example, but with the ID passed as a variable instead of as a direct part of the query string.

```
#Example query with variable

    query getEmployeeWithVariable($id: ID!) {
        getEmployees(id:$id) {
            name {
                firstname
                lastname
            }
         }
    }

    Variables:
    {
        "id": 1
    }
```

In this example, the variable is declared in the first line with (`$id: ID!`). The `!` indicates that this is a required field for this query. It is then used as an argument in the second line with (`id:$id`). Finally, the value of the variable itself is set in the variable JSON dictionary. 

### Aliases

GraphQL objects can't contain multiple properties with the same name. For example, the following query is invalid because it tries to return the product type twice.

```
#Invalid query

    query getProductDetails {
        getProduct(id: 1) {
            id
            name
        }
        getProduct(id: 2) {
            id
            name
        }
    }
```

Aliases enable you to bypass this restriction by explicitly naming the properties you want the API to return. You can use aliases to return multiple instances of the same type of object in one request. This helps to reduce the number of API calls needed.

In the example below, the query uses aliases to specify a unique name for both products. This query now passes validation, and the details are returned.

```
#Valid query using aliases

    query getProductDetails {
        product1: getProduct(id: "1") {
            id
            name
        }
        product2: getProduct(id: "2") {
            id
            name
        }
    }
```

```
#Response to query

    {
        "data": {
            "product1": {
                "id": 1,
                "name": "Juice Extractor"
             },
            "product2": {
                "id": 2,
                "name": "Fruit Overlays"
            }
        }
    }
```

**Note:** Using aliases with mutations effectively enables you to send multiple GraphQL messages in one HTTP request.

### Fragments

Fragments are reusable field subsets that can be included in multiple queries or mutations. Changes to fragments automatically apply to all queries using them.

The example below shows a `getProduct` query in which the details of the product are contained in a `productInfo` fragment.

```
#Example fragment

    fragment productInfo on Product {
        id
        name
        listed
    }
```

```
#Query calling the fragment

    query {
        getProduct(id: 1) {
            ...productInfo
            stock
        }
    }
```

```
#Response including fragment fields

    {
        "data": {
            "getProduct": {
                "id": 1,
                "name": "Juice Extractor",
                "listed": "no",
                "stock": 5
            }
        }
    }
```

## 1.7 - Subscriptions

Subscriptions establish persistent connections between client and server for real-time data updates without polling. They're ideal for chat systems, collaborative editing, and other real-time functionality.

Like queries and mutations, subscriptions define the data structure to return. They're typically implemented using WebSockets.

## 1.8 - Introspection

Introspection is a built-in GraphQL function that queries a server for schema information. It's used by GraphQL IDEs and documentation tools, allowing you to specify which schema details to return.

However, introspection poses security risks by exposing potentially sensitive API information to attackers. Best practice is to disable it in production environments.

# 2. Finding GraphQL endpoints

Before you can test a GraphQL API, you first need to find its endpoint. As GraphQL APIs use the same endpoint for all requests, this is a valuable piece of information.

## 2.1 - Universal queries

By sending `query{__typename}` to any GraphQL endpoint returns `{"data": {"__typename": "query"}}` in the response. This universal query helps identify GraphQL services, as `__typename` is a reserved field that returns the queried object's type as a string.

## 2.2 - Common endpoint names

GraphQL services often use similar endpoint suffixes. When testing for GraphQL endpoints, you should look to send universal queries to the following locations:

* `/graphql`
* `/api`
* `/api/graphql`
* `/graphql/api`
* `/graphql/graphql`

If these common endpoints don't return a GraphQL response, try appending `/v1` to the path.

**Note:** GraphQL services will often respond to any non-GraphQL request with a "query not present" or similar error.

## 2.3 - Request methods

Test different HTTP methods to find GraphQL endpoints. Production endpoints typically only accept POST requests with `application/json` content-type for CSRF protection, but some may accept GET requests or POST with `x-www-form-urlencoded`. If POST requests to common endpoints fail, retry the universal query using alternative HTTP methods.

# 3. Exploiting unsanitized arguments

Test query arguments for vulnerabilities, particularly access control issues. If APIs use arguments to access objects directly, they may be vulnerable to insecure direct object references (IDOR), where users could access unauthorized information by supplying different argument values that correspond to restricted data.

For example, the query below requests a product list for an online shop:

```
#Example product query

    query {
        products {
            id
            name
            listed
        }
    }
```

The product list returned contains only listed products.

```
#Example product response

    {
        "data": {
            "products": [
                {
                    "id": 1,
                    "name": "Product 1",
                    "listed": true
                },
                {
                    "id": 2,
                    "name": "Product 2",
                    "listed": true
                },
                {
                    "id": 4,
                    "name": "Product 4",
                    "listed": true
                }
            ]
        }
    }
```

From this information, we can infer the following:
- Products are assigned a sequential ID.
- Product ID 3 is missing from the list, possibly because it has been delisted.

By querying the ID of the missing product, we can get its details, even though it is not listed on the shop and was not returned by the original product query.

```
#Query to get missing product

    query {
        product(id: 3) {
            id
            name
            listed
        }
    }
```

```
#Missing product response

    {
        "data": {
            "product": {
            "id": 3,
            "name": "Product 3",
            "listed": no
            }
        }
    }
```

# 4. Discovering schema information

















