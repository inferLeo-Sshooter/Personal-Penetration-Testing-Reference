
# 1. GraphQL API Vulnerabilities

GraphQL vulnerabilities stem from implementation and design flaws, such as leaving introspection features active, allowing attackers to query API schemas for reconnaissance.

**Attack Types:**
- **Malicious requests** - Obtain unauthorized data or perform restricted actions
- **Query manipulation** - Gain admin privileges through crafted queries
- **CSRF exploits** - Execute cross-site request forgery attacks
- **Information disclosure** - Extract sensitive data through vulnerable endpoints

**Impact:**
These attacks can have severe consequences, particularly when attackers escalate privileges or access sensitive information through poorly secured GraphQL implementations.

## 1.1 - What is GraphQL

GraphQL is an API query language designed for efficient client-server communication. It allows users to specify exactly what data they want in responses, avoiding large response objects and multiple calls common with REST APIs.

**Key Features:**
- **Contract-based communication** - Defines how clients communicate with servers
- **Location abstraction** - Clients don't need to know where data resides
- **Single query endpoint** - GraphQL server fetches data from relevant sources
- **Platform-agnostic** - Supports multiple programming languages and data stores

GraphQL streamlines API interactions by enabling precise data requests through a unified interface.



























